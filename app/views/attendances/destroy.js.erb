alert('参加登録の解除に成功');
var tag = $('#<%= get_attend_tag_id(current_user, @sub_event_information) %>');
tag.attr('href', '<%= attend_event_information_sub_event_information_attendance_path(@event_information, @sub_event_information) %>');
tag.attr('data-method', 'post');
tag.text('参加する');

var count_tag = $('#<%= get_attendance_count_id(current_user, @sub_event_information) %>');
count_tag.text('<%= "#{@sub_event_information.attendances.count}人" %>');

var state_tag = $('#<%= get_attend_state_id(current_user, @sub_event_information) %>');
state_tag.text('不参加予定');