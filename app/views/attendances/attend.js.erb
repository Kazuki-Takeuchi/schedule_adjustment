alert('参加登録成功');
var tag = $('#<%= get_attend_tag_id(current_user, @sub_event_information) %>');
tag.attr('href', '<%= event_information_sub_event_information_attendance_path(@event_information, @sub_event_information) %>');
tag.attr('data-method', 'delete');
tag.text('参加する');

var count_tag = $('#<%= get_attendance_count_id(current_user, @sub_event_information) %>');
count_tag.text('<%= "#{@sub_event_information.attendances.count}人" %>');