%h1 ユーザ一覧

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.email
        %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }
%br
=link_to 'ホームへ', root_url

